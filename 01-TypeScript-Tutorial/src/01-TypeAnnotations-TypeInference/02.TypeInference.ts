/* ########## TYPE INFERENCE ########## */

/* ========= String Type ========= */
let name = 'shakeAndBake';
name = 'Adhiraj';
name = name.toUpperCase();
console.log(name);

// name = 25 // not allowed

/* ========= Number Type ========= */
let amount = 25;
amount = 25 - 4;
console.log(amount);

// amount ='Kavya' // not allowed

/* ========= Boolean Type ========= */
let isEligible = true;
isEligible = false;
console.log(isEligible);

// isEligible = 'not eligible' // not allowed
